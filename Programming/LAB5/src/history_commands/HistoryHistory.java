package history_commands;
/**
 * Хранит в себе имя команды {@code history}.
 */
public class HistoryHistory extends HistoryCommand {
    /**
     * Создание истории команды {@code history}.
     *
     * @param commandName название команды
     */
    public HistoryHistory(String commandName) {
        super(commandName);
    };
}
