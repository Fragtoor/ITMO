package history_commands;
/**
 * Хранит в себе имя команды {@code exit}.
 */
public class HistoryExit extends HistoryCommand {
    /**
     * Создание истории команды {@code exit}.
     *
     * @param commandName название команды
     */
    public HistoryExit(String commandName) {
        super(commandName);
    };
}
